# TODO

> 这是一个“可落地的待办清单”，用于记录当前框架的下一步方向。
> 原则：先把传统优化做扎实，再逐步推进动态优化。

## 近期优先级

- 巩固传统优化主线：Adapter / Pipeline / Bias / Plugin 的稳定组合与可复现性
- 完善可发现性与自检体验（Catalog + Suites + Run Inspector）
- 补齐高价值示例与对比基线（多策略协同/多偏置/并行评估）
- 实验批次模板插件：自动跑 N seeds，输出 batch.json / batch.csv
- 结果对比器插件：自动统计差异表（compare.csv / compare.md）

## 想法池（暂缓）

- 动态优化入口增强：在 Run Inspector 中增加“动态信号面板”用于手动触发 DynamicSwitch
  - 目标：演示/调试动态切换逻辑，避免“切换不透明/漏配”
  - 时机：传统优化主线稳定后再做

- Run Inspector 向“实验平台”演化（本地版）
  - 实验管理：运行历史分组、标签、对比、一键导出对比表
  - 参数面板：在线调整关键参数并保存为实验模板
  - 偏置/插件可视化配置：权重/阈值面板 + 缺失伙伴提示
  - 结果可视化：best 曲线 / Pareto 简图 / 关键统计

- 实验平台服务化（服务器版）
  - 实验结果写入数据库或对象存储
  - 历史实验检索 / 对比 / 回溯
  - 本地 UI 作为前端，服务端提供实验资产

- 组合类型系统与算法 DSL（静态验证 + 解释装配）
  - 扩展 catalog：组件 provides/requires
  - 组合静态检查器：装配前验证缺失/冲突/顺序
  - 声明式算法描述语言（YAML/JSON）
  - 可选“编译模式”：将 DSL 转为具体 solver 实例

- 敏感性分析（基础已实现，后续增强）
  - 支持多参数联合采样（而非单参数 sweep）
  - 支持随机采样 / 拉丁超立方 / Sobol
  - 支持统计汇总与可视化（均值/方差/置信区间）
  - 与 Run Inspector 深度集成：结果面板 + 历史对比

- 前沿局部优化方向（逐步集成）
  - DFO: 信赖域 + 代理模型（含低秩/子空间）
  - 非光滑/复杂约束的信赖域方法
  - 局部搜索的自动设计（LSTM/神经进化）
  - MAS（Model-and-Search）类方法

