# docs/user_guide

用途：用户使用指南（具体操作与流程）。
边界：面向使用，不包含内部设计。
示例：RUN_INSPECTOR、PLUGIN_SYSTEM。

Purpose: user guides and how-to docs.
Boundary: usage only.
Example: RUN_INSPECTOR, PLUGIN_SYSTEM.

## Key Guides

- `PROJECT_SCAFFOLD.md`: create project skeleton and local catalog workflow.
- `RUN_INSPECTOR.md`: UI inspection/search workflow.
- `CONTEXT_CONTRACTS.md`: requires/provides/mutates contracts.
- `PLUGIN_SELECTION.md`: plugin decision guide (when to use which plugin).
- `DB_EVENT_LOGGING.md`: MySQL event logging schema and rollout plan.
