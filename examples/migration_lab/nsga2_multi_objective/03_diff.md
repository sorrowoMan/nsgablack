# Traditional NSGA-II -> Framework Mapping / ?? NSGA-II ?????

This file maps the baseline implementation (`00_baseline.py`) to the framework implementation (`02_framework_final.py`).
????????`00_baseline.py`?????????`02_framework_final.py`??

## Mapping Table / ???

| Baseline responsibility / ???? | Baseline location / ???? | Framework replacement / ???? | Why this helps / ?? |
|---|---|---|---|
| Objective pair / ????? | `evaluate_individual` | `BiObjectiveShiftedSphere.evaluate` | Keep objective semantics in Problem layer. / ??????? Problem ?? |
| Non-dominated sort / ????? | `fast_non_dominated_sort` | `NSGA2Adapter.update` internal ranking | Process detail moves into adapter. / ????? adapter ??? |
| Crowding distance / ???? | `crowding_distance` | `NSGA2Adapter` internal selection | Selection policy stays encapsulated. / ?????????????? |
| Tournament + crossover + mutation / ?????? | baseline loop | `NSGA2Adapter.propose` + pipeline mutator | Process and operator concerns are decoupled. / ?????????? |
| Environmental selection / ???? | `environmental_selection` | `NSGA2Adapter.update` | Survivor selection logic becomes reusable. / ?????????? |
| Runtime loop / ??? | `run_baseline` | `ComposableSolver.run` | Unified lifecycle and plugin/context hooks. / ???????????????? |
| Seed / ???? | local RNG only | `solver.set_random_seed` | Framework-level reproducibility entry. / ????????? |

## Minimal Migration Checklist / ??????

1. Move objective definitions to a `BlackBoxProblem` subclass. / ??????? `BlackBoxProblem` ???
2. Move operator components to `RepresentationPipeline`. / ????? `RepresentationPipeline`?
3. Replace manual NSGA-II loop with `NSGA2Adapter`. / ? `NSGA2Adapter` ???? NSGA-II ????
4. Assemble via `ComposableSolver(problem, adapter, pipeline)`. / ?? `ComposableSolver(problem, adapter, pipeline)` ???
5. Run with `set_max_steps` and `run()`, then inspect Pareto front. / ? `set_max_steps` + `run()` ????? Pareto ???

## Notes / ??

- This workshop focuses on process migration, not adding bias/plugin yet. / ?????????????? bias/plugin?
- Next step: add one domain bias and one runtime plugin while keeping adapter unchanged. / ???????? adapter ???????? bias ???? plugin?
