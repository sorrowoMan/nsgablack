# Canonical Case tool catalog shard.

[[entry]]
key = "project.problem.example"
title = "SphereProblem"
kind = "tool"
import_path = "problem.example_problem:SphereProblem"
tags = ["project", "example", "problem"]
summary = "Example two-objective problem."
companions = ["project.pipeline.default"]
context_requires = []
context_provides = []
context_mutates = []
context_cache = []
context_notes = ["Defines objective/constraint semantics only; no runtime context writes."]
use_when = ["Define project objective/constraint semantics."]
minimal_wiring = ["from problem.example_problem import SphereProblem"]
required_companions = ["project.pipeline.default"]
config_keys = ["dimension"]
example_entry = "examples.cases.dynamic_strategy.cases.dynamic_strategy.build_solver:build_solver"
