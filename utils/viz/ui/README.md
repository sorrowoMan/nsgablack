# utils/viz/ui

用途：可视化 UI 子视图（运行、贡献、目录、上下文等）。
边界：视图层，不包含业务逻辑。
示例：`run_view.py`, `contrib_view.py`。

Purpose: UI subviews for Run Inspector.
Boundary: view layer only.
Example: `run_view.py`, `contrib_view.py`.
