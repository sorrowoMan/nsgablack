"""Canonical solver case assembly entry (nsgablack)."""

from nsgablack.core.base import BlackBoxProblem
from nsgablack.adapters.algorithm_adapter import AlgorithmAdapter
from nsgablack.representation.base import RepresentationPipeline
from nsgablack.bias.core.base import BiasBase
from nsgablack.plugins.base import Plugin


def build_solver(config=None, *, resource_context=None, component_overrides=None):
    """Assemble and return one solver case.

    Components inherit from blackbase ABC base classes via nsgablack:
      - Problem: BlackBoxProblem(ProblemBase) → implement evaluate(x)
      - Adapter: AlgorithmAdapter(AdapterBase) → implement propose/update
      - Representation: RepresentationPipeline(RepresentationBase) → wire slots
      - Bias: BiasBase → implement compute(x, context)
      - Plugin: Plugin(PluginBase) → override lifecycle hooks

    ``resource_context`` is granted by the project-level L0 substrate.
    """
    del config, resource_context, component_overrides
    raise NotImplementedError("TODO: implement build_solver")
