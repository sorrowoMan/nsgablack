# bias/surrogate

用途：代理模型相关偏置（预算、阶段、风险控制等）。
边界：依赖代理评估插件，不直接训练模型。
示例：`SurrogateControlBias` 等。

Purpose: surrogate-related biases (budget, phase, risk control).
Boundary: depends on surrogate evaluation; no model training.
Example: `SurrogateControlBias`.
