# bias/algorithmic/signal_driven

用途：信号驱动偏置（依赖统计/评估信号动态调整）。
边界：不提供信号本身，仅消费信号。
示例：基于 MonteCarlo/统计插件的偏置。

Purpose: signal-driven biases (consume runtime signals).
Boundary: consumes signals; does not generate them.
Example: MC-signal-based biases.
