# ExampleDomainBias

Template for a domain (business) bias.

## Purpose
- Show how to implement `DomainBias`.
- Encodes a soft business rule (sum limit).

## File
- `bias/domain/template_domain_bias.py`

## Usage
Instantiate and register with a bias manager, or wrap it into a function bias.
